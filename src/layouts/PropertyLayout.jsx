function PropertyLayout({ hero, title, location, host, rate, details }) {
    return (
        <>
            {hero}
            {title}
            {location}
            {host}
            {rate}
            {details}
        </>
    )
}

export default PropertyLayout;